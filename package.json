{
  "name": "daily-space-stock-signals",
  "version": "1.0.0",
  "private": true,
  "description": "Daily Space with a US stock watchlist, financial events, and market signals.",
  "scripts": {
    "start": "node --experimental-sqlite --disable-warning=ExperimentalWarning server/index.js",
    "dev": "node --experimental-sqlite --disable-warning=ExperimentalWarning --watch server/index.js",
    "test": "node --experimental-sqlite --disable-warning=ExperimentalWarning --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@google/design.md": "^0.3.0",
    "@supabase/supabase-js": "^2.110.2"
  }
}
